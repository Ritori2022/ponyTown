# 🚀 快速启动方案对比

## 方案对比

| 方案 | 构建时间 | 使用场景 | 命令 |
|------|---------|---------|------|
| **标准开发模式** | 20-30分钟（首次） | 完整开发环境 | `docker-compose.dev.yml` |
| **快速模式** | 2-5分钟 | 快速测试 | `docker-compose.dev.fast.yml` |
| **本地直接运行** | 无需Docker | 开发调试 | `npm start` |

---

## 方案1：等待标准构建完成（推荐新手）⭐

**优点**：一次构建，以后秒启动
**缺点**：首次需要20-30分钟

```bash
# 继续等待当前构建
# 查看进度
docker compose -f docker-compose.dev.yml logs -f
```

---

## 方案2：使用快速构建（适合急用）🚀

**优点**：只需2-5分钟
**缺点**：需要先在本地编译代码

### 步骤：

```bash
# 1. 停止当前构建
docker compose -f docker-compose.dev.yml down

# 2. 安装依赖（首次需要5-10分钟）
npm install --legacy-peer-deps

# 3. 编译代码（约2分钟）
npm run ts

# 4. 构建前端（可选，约3分钟）
npm run build

# 5. 使用快速Docker配置启动（2分钟）
docker compose -f docker-compose.dev.fast.yml up -d

# 6. 创建测试账户
./create-test-account.sh "测试" "test@example.com"
```

---

## 方案3：本地直接运行（最快）⚡

**优点**：无需Docker，启动最快
**缺点**：需要本地安装MongoDB

### 步骤：

```bash
# 1. 安装MongoDB
brew install mongodb-community

# 2. 启动MongoDB
brew services start mongodb-community

# 3. 配置数据库
mongosh
> use ponytown
> db.createUser({
    user: "admin",
    pwd: "changeme123",
    roles: [{role: "readWrite", db: "ponytown"}]
  })
> exit

# 4. 修改config.dev.json的db连接
# "db": "mongodb://admin:changeme123@localhost:27017/ponytown?authSource=admin"

# 5. 安装依赖
npm install --legacy-peer-deps

# 6. 编译代码
npm run ts

# 7. 启动服务器
node pony-town.js --login --admin --game --local

# 8. 访问 http://localhost:8090
```

---

## 推荐流程

### 如果你是第一次使用：
1. **现在**: 让Docker继续构建（再等10-20分钟）
2. **以后**: 直接 `docker compose -f docker-compose.dev.yml up -d`（秒启动）

### 如果你很着急：
1. **现在**: 停止Docker，使用方案2或3
2. **晚上**: 让Docker在后台慢慢构建（方案1）

---

## 加速Docker构建的技巧

### 1. 增加Docker资源

打开Docker Desktop → Settings → Resources：
- **CPUs**: 增加到4核或更多
- **Memory**: 增加到8GB或更多
- **Disk**: 确保有足够空间（至少20GB）

### 2. 使用国内镜像源（中国用户）

创建 `/etc/docker/daemon.json`：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://registry.docker-cn.com"
  ]
}
```

重启Docker Desktop

### 3. 清理Docker缓存

```bash
# 清理所有未使用的镜像和容器
docker system prune -a

# 然后重新构建
docker compose -f docker-compose.dev.yml build --no-cache
```

---

## 常见问题

### Q: 构建卡住不动了？
A: 等待5分钟，可能只是下载慢。如果真的卡住：
```bash
Ctrl+C 停止
docker compose -f docker-compose.dev.yml down
docker compose -f docker-compose.dev.yml up -d --build
```

### Q: 第二次启动还需要这么久吗？
A: 不需要！构建完成后会缓存，以后只需要几秒钟。

### Q: 我修改了代码，需要重新构建吗？
A: 看情况：
- 修改TypeScript代码：需要 `npm run ts` 重新编译
- 修改config.json：只需 `docker compose restart`
- 修改Dockerfile：需要 `docker compose build`

---

选择适合你的方案开始吧！ 🎮
